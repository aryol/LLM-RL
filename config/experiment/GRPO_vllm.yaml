# @package _global_

default: 
  - GRPO

trainer:
  args:
    use_vllm: true
    vllm_device: "cuda:7"
    vllm_server_host: "localhost"
    vllm_server_port: 8000
    vllm_gpu_memory_utilization: 0.9
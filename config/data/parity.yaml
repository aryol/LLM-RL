defaults:
  - default

name: parity

train_files: data/verl-data/parity/train.parquet
val_files: data/verl-data/parity/test.parquet

max_prompt_length: 2048
max_response_length: 1536
train_batch_size: 128

compute_score_fn: src.utils.rewards.ParityReward